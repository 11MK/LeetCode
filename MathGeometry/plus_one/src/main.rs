
pub fn plus_one(digits: Vec<i32>) -> Vec<i32> {
    let result: Vec<i32> = Vec::new();
    let s = digits.clone();
    let s = s.iter().clone()
        .collect::<String>();

    result
}

fn main() {
    plus_one 
}
